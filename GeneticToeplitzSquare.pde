/*
GeneticToeplitzSquare
*/

// Sets up some initial values.
public void settings() {
  frameRate(60);
  noSmooth();
  size(1000, 600);
}

// Runs once on the first frame.
public void setup() {
  
}

// Runs each frame.
public void draw() {
  
}
